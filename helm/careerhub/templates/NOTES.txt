CareerHub {{ .Chart.AppVersion }} has been deployed as release "{{ .Release.Name }}".

Images in use:
  backend   {{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}
  frontend  {{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}

1. Watch the pods come up:

   kubectl -n {{ include "careerhub.namespace" . }} get pods -w

{{- if .Values.ingress.enabled }}

2. Point {{ .Values.ingress.host }} at your ingress controller, then open:

   http://{{ .Values.ingress.host }}/
   http://{{ .Values.ingress.host }}/swagger-ui.html
{{- else }}

2. The ingress is disabled. Port-forward instead:

   kubectl -n {{ include "careerhub.namespace" . }} port-forward svc/{{ include "careerhub.name" . }}-frontend 8080:{{ .Values.frontend.service.port }}
{{- end }}

3. If you are still using the default secrets, replace them before going further:

   helm upgrade {{ .Release.Name }} . \
     --set secrets.dbPassword="$(openssl rand -base64 24)" \
     --set secrets.jwtSecret="$(openssl rand -base64 48)"
